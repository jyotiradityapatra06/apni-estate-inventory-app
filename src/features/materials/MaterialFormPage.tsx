import { useEffect, useRef, useState } from "react";
import { ChevronDown } from "lucide-react";
import { useNavigate, useParams } from "react-router";
import { toast } from "sonner";
import { godownApi } from "../../api/godown.api";
import { inventoryApi, type InventoryItemData } from "../../api/inventory.api";
import { supplierApi } from "../../api/supplier.api";
import { ErrorState, LoadingSkeleton } from "../../app/components/common/FeedbackStates";
import { PageHeader, SectionHeader } from "../../app/components/common/PageHeader";
import type { Godown } from "../../types/godown.types";
import type { Supplier } from "../../types/supplier.types";

const empty = { materialName: "", category: "", brand: "", sku: "", unit: "", minimumStockLevel: "", costPrice: "", sellingPrice: "", hsnCode: "", taxRate: "", defaultSupplierId: "", godownId: "", openingStock: "0" };
export function MaterialFormPage({ mode }: { mode: "create" | "edit" }) {
  const { id = "" } = useParams(); const navigate = useNavigate(); const firstRef = useRef<HTMLInputElement>(null); const [form, setForm] = useState(empty); const [material, setMaterial] = useState<InventoryItemData | null>(null); const [godowns, setGodowns] = useState<Godown[]>([]); const [suppliers, setSuppliers] = useState<Supplier[]>([]); const [more, setMore] = useState(false); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState("");
  useEffect(() => { let active = true; Promise.all([godownApi.getAll(), supplierApi.getAll(), ...(mode === "edit" ? [inventoryApi.getItem(id)] : [])]).then((responses: any[]) => { if (!active) return; setGodowns((responses[0].data ?? []).filter((item: Godown) => item.isActive)); setSuppliers((responses[1].data ?? []).filter((item: Supplier) => item.isActive)); if (mode === "edit") { const item = responses[2].data as InventoryItemData; setMaterial(item); setForm({ materialName: item.materialName, category: item.category, brand: item.brand ?? "", sku: item.sku, unit: item.unit, minimumStockLevel: String(item.minimumStockLevel ?? item.reorderLevel ?? 0), costPrice: item.costPrice == null ? "" : String(item.costPrice), sellingPrice: item.sellingPrice == null ? "" : String(item.sellingPrice), hsnCode: item.hsnCode ?? "", taxRate: item.taxRate == null ? "" : String(item.taxRate), defaultSupplierId: item.defaultSupplierId ?? "", godownId: "", openingStock: "0" }); setMore(Boolean(item.hsnCode || item.taxRate != null)); } else if (responses[0].data?.length === 1) setForm((value) => ({ ...value, godownId: responses[0].data[0].id })); }).catch((error) => setError(error instanceof Error ? error.message : "Could not load the form.")).finally(() => setLoading(false)); return () => { active = false; }; }, [id, mode]);
  const change = (key: keyof typeof empty, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setError(""); if (!form.materialName.trim() || !form.category.trim() || !form.sku.trim() || !form.unit.trim()) { setError("Material name, category, SKU and unit are required."); firstRef.current?.focus(); firstRef.current?.scrollIntoView({ behavior: "smooth", block: "center" }); return; } setSaving(true); const body: any = { materialName: form.materialName.trim(), category: form.category.trim(), brand: form.brand.trim() || null, sku: form.sku.trim(), unit: form.unit.trim(), minimumStockLevel: Number(form.minimumStockLevel || 0), reorderLevel: Number(form.minimumStockLevel || 0), location: material?.location || godowns.find((item) => item.id === form.godownId)?.name || "Main Godown", costPrice: form.costPrice ? Number(form.costPrice) : null, sellingPrice: form.sellingPrice ? Number(form.sellingPrice) : null, hsnCode: form.hsnCode.trim() || null, taxRate: form.taxRate ? Number(form.taxRate) : null, defaultSupplierId: form.defaultSupplierId || null, supplierIds: form.defaultSupplierId ? [form.defaultSupplierId] : [] }; if (mode === "create") { body.quantity = Number(form.openingStock || 0); body.openingStock = Number(form.openingStock || 0); if (form.godownId) body.godownId = form.godownId; } try { const response = mode === "create" ? await inventoryApi.createItem(body) : await inventoryApi.updateItem(id, body); toast.success(mode === "create" ? "Material added" : "Material updated"); navigate(`/materials/${response.data.id}`, { replace: true }); } catch (error) { setError(error instanceof Error ? error.message : "Material could not be saved."); } finally { setSaving(false); } };
  if (loading) return <LoadingSkeleton rows={6}/>; if (error && mode === "edit" && !material) return <ErrorState message={error} onRetry={() => window.location.reload()}/>;
  const inputClass = "mt-2 w-full rounded-lg border border-slate-300 bg-white px-3 text-base focus:border-blue-600 focus:ring-2 focus:ring-blue-100";
  return <form onSubmit={submit} className="mx-auto max-w-4xl space-y-6 pb-24"><PageHeader title={mode === "create" ? "Add Material" : "Edit Material"} description={mode === "create" ? "Add a material and its opening stock." : "Update material details. Use Stock In or Stock Out to change quantity."}/>{error && <p className="rounded-lg border border-red-200 bg-red-50 p-3 text-[14px] font-semibold text-red-800">{error}</p>}<section className="rounded-xl border bg-white p-4 shadow-sm md:p-6"><SectionHeader title="Basic Details"/><div className="mt-4 grid gap-4 md:grid-cols-2"><label className="text-[14px] font-semibold">Material name<input ref={firstRef} required value={form.materialName} onChange={(e) => change("materialName", e.target.value)} className={inputClass}/></label><label className="text-[14px] font-semibold">Category<input required value={form.category} onChange={(e) => change("category", e.target.value)} placeholder="Cement, Steel, Sand…" className={inputClass}/></label><label className="text-[14px] font-semibold">Unit<input required value={form.unit} onChange={(e) => change("unit", e.target.value)} placeholder="Bags, Tonnes, Pieces…" className={inputClass}/></label><label className="text-[14px] font-semibold">SKU / Item Code<input required value={form.sku} onChange={(e) => change("sku", e.target.value)} className={inputClass}/></label><label className="text-[14px] font-semibold">Brand<input value={form.brand} onChange={(e) => change("brand", e.target.value)} className={inputClass}/></label><label className="text-[14px] font-semibold">Minimum stock level<input type="number" min="0" step="0.001" value={form.minimumStockLevel} onChange={(e) => change("minimumStockLevel", e.target.value)} className={inputClass}/></label></div></section><section className="rounded-xl border bg-white p-4 shadow-sm md:p-6"><SectionHeader title="Pricing and Supplier"/><div className="mt-4 grid gap-4 md:grid-cols-3"><label className="text-[14px] font-semibold">Purchase price<input type="number" min="0.01" step="0.01" value={form.costPrice} onChange={(e) => change("costPrice", e.target.value)} className={inputClass}/></label><label className="text-[14px] font-semibold">Selling price<input type="number" min="0.01" step="0.01" value={form.sellingPrice} onChange={(e) => change("sellingPrice", e.target.value)} className={inputClass}/></label><label className="text-[14px] font-semibold">Preferred supplier<select value={form.defaultSupplierId} onChange={(e) => change("defaultSupplierId", e.target.value)} className={inputClass}><option value="">No preferred supplier</option>{suppliers.map((supplier) => <option key={supplier.id} value={supplier.id}>{supplier.name}</option>)}</select></label></div></section>{mode === "create" && <section className="rounded-xl border bg-white p-4 shadow-sm md:p-6"><SectionHeader title="Opening Stock" description="This creates the first stock-history entry."/><div className="mt-4 grid gap-4 md:grid-cols-2"><label className="text-[14px] font-semibold">Godown<select required value={form.godownId} onChange={(e) => change("godownId", e.target.value)} className={inputClass}><option value="">Choose godown</option>{godowns.map((godown) => <option key={godown.id} value={godown.id}>{godown.name}</option>)}</select></label><label className="text-[14px] font-semibold">Opening quantity<input type="number" min="0" step="0.001" value={form.openingStock} onChange={(e) => change("openingStock", e.target.value)} className={inputClass}/></label></div></section>}<section className="rounded-xl border bg-white"><button type="button" onClick={() => setMore(!more)} className="flex min-h-14 w-full items-center justify-between px-4 text-[15px] font-bold">More Details<ChevronDown className={more ? "rotate-180" : ""}/></button>{more && <div className="grid gap-4 border-t p-4 md:grid-cols-2"><label className="text-[14px] font-semibold">HSN code<input value={form.hsnCode} onChange={(e) => change("hsnCode", e.target.value)} className={inputClass}/></label><label className="text-[14px] font-semibold">GST rate %<input type="number" min="0" max="100" step="0.01" value={form.taxRate} onChange={(e) => change("taxRate", e.target.value)} className={inputClass}/></label></div>}</section><div className="fixed inset-x-0 bottom-[72px] z-30 flex gap-3 border-t bg-white p-3 lg:static lg:justify-end lg:border-0 lg:bg-transparent"><button type="button" onClick={() => navigate(-1)} className="min-h-12 flex-1 rounded-lg border px-5 font-semibold lg:flex-none">Cancel</button><button disabled={saving} className="min-h-12 flex-[2] rounded-lg bg-blue-700 px-6 font-semibold text-white disabled:opacity-60 lg:flex-none">{saving ? "Saving…" : mode === "create" ? "Add Material" : "Save Changes"}</button></div></form>;
}
