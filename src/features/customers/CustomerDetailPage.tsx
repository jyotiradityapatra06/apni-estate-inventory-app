import{useEffect,useState}from"react";import{Link,useParams}from"react-router";import{Mail,Phone}from"lucide-react";import{customerApi}from"../../api/customer.api";import{PageHeader,SectionHeader}from"../../app/components/common/PageHeader";import{useAuth}from"../../hooks/useAuth";import type{Customer}from"../../types/customer.types";import{hasPermission}from"../../utils/permissions";const money=(n:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR"}).format(n||0);
export function CustomerDetailPage(){const{id=""}=useParams();const{user}=useAuth();const[data,setData]=useState<(Customer&{transactionHistory?:unknown[]})|null>(null),[error,setError]=useState("");useEffect(()=>{customerApi.getById(id).then(r=>setData(r.data)).catch(e=>setError(e.message))},[id]);if(error)return <div className="rounded-xl bg-red-50 p-5 text-red-800">{error}</div>;if(!data)return <div className="h-64 animate-pulse rounded-xl bg-slate-200"/>;return <div className="space-y-5"><PageHeader title={data.name} description={`${data.customerCode} · ${data.phone}`} actions={<div className="flex flex-wrap items-center gap-2"><a href={`tel:${data.phone}`} className="flex min-h-10 items-center justify-center gap-2 rounded-xl border border-slate-200 bg-white px-4 text-xs font-bold text-slate-700 shadow-sm hover:bg-slate-50 cursor-pointer"><Phone size={14}/>Call</a>{hasPermission(user,"sales:manage")&&<Link to={`/sales-orders/new?customerId=${data.id}`} className="flex min-h-10 items-center justify-center gap-2 rounded-xl bg-orange-600 hover:bg-orange-700 px-4 text-xs font-bold text-white shadow-sm transition-colors cursor-pointer">Create Sale</Link>}{hasPermission(user,"financials:manage")&&<Link to={`/payments/new?customerId=${data.id}`} className="flex min-h-10 items-center justify-center gap-2 rounded-xl bg-[#0F172A] hover:bg-slate-800 px-4 text-xs font-bold text-white shadow-sm transition-colors cursor-pointer">Receive Payment</Link>}{hasPermission(user,"customers:update")&&<Link to={`/customers/${data.id}/edit`} className="flex min-h-10 items-center justify-center gap-2 rounded-xl border border-slate-200 bg-white px-4 text-xs font-bold text-slate-700 shadow-sm hover:bg-slate-50 transition-colors cursor-pointer">Edit profile</Link>}</div>}/><div className="grid grid-cols-2 gap-3 lg:grid-cols-3"><Card label="Amount to Receive" value={money(data.outstandingBalance)}/><Card label="Credit Limit" value={money(data.creditLimit)}/><Card label="Opening Balance" value={money(data.openingBalance)}/></div><section className="rounded-xl border bg-white p-5"><SectionHeader title="Contact and Business Details"/><dl className="mt-4 grid gap-4 md:grid-cols-2">{data.phone&&<Row label="Phone Number"><a href={`tel:${data.phone}`} className="text-blue-700">{data.phone}</a></Row>}{data.email&&<Row label="Email"><a href={`mailto:${data.email}`} className="inline-flex items-center gap-1 text-blue-700"><Mail size={15}/>{data.email}</a></Row>}{data.gstin&&<Row label="GST Number">{data.gstin}</Row>}{data.billingAddress&&<Row label="Billing Address">{data.billingAddress}</Row>}{data.shippingAddress&&<Row label="Delivery Address">{data.shippingAddress}</Row>}{data.notes&&<Row label="Notes">{data.notes}</Row>}</dl></section></div>};function Card({label,value}:{label:string;value:string}){return <article className="rounded-xl border bg-white p-4"><p className="text-sm text-slate-500">{label}</p><p className="mt-1 break-words text-xl font-bold">{value}</p></article>};function Row({label,children}:{label:string;children:React.ReactNode}){return <div><dt className="text-xs font-bold uppercase tracking-wide text-slate-500">{label}</dt><dd className="mt-1 break-words text-sm font-semibold">{children}</dd></div>}
