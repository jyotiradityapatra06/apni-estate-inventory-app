import { useEffect,useRef,useState } from "react";import{useNavigate,useParams}from"react-router";import{toast}from"sonner";import{customerApi}from"../../api/customer.api";import{PageHeader,SectionHeader}from"../../app/components/common/PageHeader";import type{CustomerInput}from"../../types/customer.types";
const empty:CustomerInput={name:"",phone:"",email:"",gstin:"",billingAddress:"",shippingAddress:"",creditLimit:0,openingBalance:0,notes:"",isActive:true};const cls="mt-2 min-h-12 w-full rounded-lg border px-3 text-base";const phone=/^(?:\+91[ -]?)?[6-9]\d{9}$/;const gst=/^[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z][1-9A-Z]Z[0-9A-Z]$/;
export function CustomerFormPage({mode}:{mode:"create"|"edit"}){const{id=""}=useParams();const nav=useNavigate();const first=useRef<HTMLInputElement>(null);const[form,setForm]=useState<CustomerInput>(empty),[loading,setLoading]=useState(mode==="edit"),[saving,setSaving]=useState(false),[error,setError]=useState("");useEffect(()=>{if(mode==="edit")customerApi.getById(id).then(r=>{const c=r.data;setForm({name:c.name,phone:c.phone,email:c.email||"",gstin:c.gstin||"",billingAddress:c.billingAddress||"",shippingAddress:c.shippingAddress||"",creditLimit:c.creditLimit,openingBalance:c.openingBalance,notes:c.notes||"",isActive:c.isActive})}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[id,mode]);const set=<K extends keyof CustomerInput>(k:K,v:CustomerInput[K])=>setForm(f=>({...f,[k]:v}));const submit=async(e:React.FormEvent)=>{e.preventDefault();setError("");if(!phone.test(form.phone.replace(/\s/g,""))){setError("Enter a valid Indian phone number.");first.current?.scrollIntoView({behavior:"smooth",block:"center"});return}if(form.gstin&&!gst.test(form.gstin)){setError("Enter a valid 15-character GST number.");return}setSaving(true);try{const r=mode==="create"?await customerApi.create(form):await customerApi.update(id,form);toast.success(mode==="create"?"Customer added":"Customer updated");nav(`/customers/${r.data.id}`)}catch(e){setError(e instanceof Error?e.message:"Customer could not be saved.")}finally{setSaving(false)}};if(loading)return <div className="h-64 animate-pulse rounded-xl bg-slate-200"/>;return <form onSubmit={submit} className="mx-auto max-w-4xl space-y-5 pb-24"><PageHeader title={mode==="create"?"Add Customer":"Edit Customer"} description="Add contact, GST and address details."/>{error&&<p className="rounded-lg border border-red-200 bg-red-50 p-3 font-semibold text-red-800">{error}</p>}<section className="rounded-xl border bg-white p-4 md:p-6"><SectionHeader title="Basic Information"/><div className="mt-4 grid gap-4 md:grid-cols-2"><label className="text-sm font-semibold">Customer name<input ref={first} required value={form.name} onChange={e=>set("name",e.target.value)} className={cls}/></label><label className="text-sm font-semibold">Phone Number<input required inputMode="tel" value={form.phone} onChange={e=>set("phone",e.target.value)} className={cls}/></label><label className="text-sm font-semibold">Email<input type="email" value={form.email||""} onChange={e=>set("email",e.target.value)} className={cls}/></label><label className="text-sm font-semibold">GST Number<input maxLength={15} value={form.gstin||""} onChange={e=>set("gstin",e.target.value.toUpperCase())} className={cls}/></label></div></section><section className="rounded-xl border bg-white p-4 md:p-6"><SectionHeader title="Address"/><div className="mt-4 grid gap-4 md:grid-cols-2"><label className="text-sm font-semibold">Billing Address<textarea value={form.billingAddress||""} onChange={e=>set("billingAddress",e.target.value)} className={`${cls} min-h-24 p-3`}/></label><label className="text-sm font-semibold">Delivery Address<textarea value={form.shippingAddress||""} onChange={e=>set("shippingAddress",e.target.value)} className={`${cls} min-h-24 p-3`}/></label></div></section><section className="rounded-xl border bg-white p-4 md:p-6"><SectionHeader title="Credit Details"/><div className="mt-4 grid gap-4 md:grid-cols-2"><label className="text-sm font-semibold">Credit limit<input type="number" min="0" step="0.01" value={form.creditLimit} onChange={e=>set("creditLimit",Number(e.target.value))} className={cls}/></label><label className="text-sm font-semibold">Opening outstanding<input type="number" min="0" step="0.01" value={form.openingBalance} onChange={e=>set("openingBalance",Number(e.target.value))} className={cls}/></label></div></section><div className="fixed inset-x-0 bottom-[72px] z-30 flex gap-3 border-t bg-white p-3 md:static md:justify-end md:border-0 md:bg-transparent"><button type="button" onClick={()=>nav(-1)} className="min-h-12 flex-1 rounded-lg border px-5 font-semibold md:flex-none">Cancel</button><button disabled={saving} className="min-h-12 flex-[2] rounded-lg bg-blue-700 px-6 font-semibold text-white disabled:opacity-60 md:flex-none">{saving?"Saving…":mode==="create"?"Add Customer":"Save Changes"}</button></div></form>}
