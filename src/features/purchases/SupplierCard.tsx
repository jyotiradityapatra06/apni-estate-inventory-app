import{Link}from"react-router";import type{Supplier}from"../../types/supplier.types";import{fmt}from"../../utils/currency";export function SupplierCard({supplier}:{supplier:Supplier}){return <article className="rounded-xl border bg-white p-4"><div className="flex justify-between gap-3"><div><h2 className="text-lg font-bold">{supplier.name}</h2><p className="text-xs text-blue-700">{supplier.supplierCode}</p></div><span className={`h-fit rounded-full px-2 py-1 text-xs font-bold ${supplier.isActive?"bg-emerald-50 text-emerald-800":"bg-slate-100"}`}>{supplier.isActive?"Active":"Inactive"}</span></div><p className="mt-3">{supplier.phone}</p><div className="mt-3 flex justify-between text-sm"><span>Outstanding</span><b>{fmt(supplier.pendingPayments)}</b></div><Link to={`/suppliers/${supplier.id}`} className="mt-4 flex min-h-11 items-center justify-center rounded-lg border font-semibold">View Supplier</Link></article>}
