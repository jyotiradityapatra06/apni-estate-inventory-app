export type ExpenseStatus = "PENDING" | "PAID" | "CANCELLED";
export type ExpensePaymentMode = "CASH" | "UPI" | "BANK_TRANSFER" | "CHEQUE" | "CARD" | "OTHER";
export interface ExpenseCategory { id:string; name:string; description?:string|null; isActive:boolean; isDefault:boolean; _count?:{expenses:number}; }
export interface Expense { id:string; expenseNumber:string; expenseDate:string; categoryId:string; category:ExpenseCategory; payee?:string|null; amount:number|string; gstApplicable:boolean; gstRate:number|string; gstAmount:number|string; totalAmount:number|string; paymentMode?:ExpensePaymentMode|null; paymentStatus:ExpenseStatus; notes?:string|null; attachmentUrl?:string|null; paidAt?:string|null; cancelledAt?:string|null; cancellationReason?:string|null; cancelledBy?:{id:string;name:string}|null; createdBy:{id:string;name:string}; createdAt:string; ledgerEntries?:Array<{id:string;transactionType:string;debitAmount:number|string;creditAmount:number|string;description:string;transactionDate:string}>; }
export interface ExpenseSummary { todayExpenses:number; monthExpenses:number; totalPaid:number; pendingExpenses:number; }
export interface ExpenseInput { expenseDate:string; categoryId:string; payee:string; amount:number; gstApplicable:boolean; gstRate:number; paymentStatus:"PENDING"|"PAID"; paymentMode:ExpensePaymentMode|""; notes:string; attachmentUrl:string; idempotencyKey?:string; }
