import{useEffect,useState}from"react";import{Plus}from"lucide-react";import{useNavigate}from"react-router";import paymentApi from"../../api/payment.api";import{BusinessStatusBadge}from"../../app/components/common/BusinessStatusBadge";import{fmt}from"../../utils/currency";import{useAuth}from"../../hooks/useAuth";import{hasPermission}from"../../utils/permissions";
export default function PaymentsPage(){const nav=useNavigate();const{user}=useAuth();const[data,setData]=useState<any[]>([]);const[error,setError]=useState("");useEffect(()=>{paymentApi.getAll().then(r=>setData(r.data||[])).catch((e:any)=>setError(e.message))},[]);return <div className="space-y-5"><div className="flex justify-between"><div><h1 className="text-2xl font-bold md:text-3xl">Payments</h1><p className="mt-1 text-base text-slate-600">Money received from customers</p></div>{user&&hasPermission(user,"sales:manage")&&<button onClick={()=>nav("/payments/new")} className="flex min-h-12 items-center gap-2 rounded-lg bg-blue-700 px-4 font-semibold text-white"><Plus size={20}/>Record Payment</button>}</div>{error&&<div className="rounded-xl bg-red-50 p-4 text-red-800">{error}</div>}<div className="grid gap-3">{data.map(p=><button key={p.id} onClick={()=>nav(`/payments/${p.id}`)} className="rounded-xl border bg-white p-4 text-left shadow-sm"><div className="flex justify-between"><div><p className="text-lg font-bold">{p.paymentNumber}</p><p className="mt-1">{p.customer?.name}</p><p className="text-sm text-slate-500">{p.invoice?.invoiceNumber} • {p.paymentMethod.replaceAll("_"," ")}</p></div><div className="text-right"><BusinessStatusBadge status={p.status}/><p className="mt-3 text-xl font-bold text-green-700">{fmt(p.amount)}</p></div></div></button>)}{!data.length&&!error&&<div className="rounded-xl border border-dashed bg-white p-10 text-center text-slate-600">No customer payments recorded.</div>}</div></div>}
