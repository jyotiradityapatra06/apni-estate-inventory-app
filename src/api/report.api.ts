import{apiClient}from"./apiClient";export interface ReportResponse{success:boolean;summary:Record<string,unknown>;breakdowns:Record<string,Array<{name:string;total:number}>>;rows:Array<Record<string,unknown>>;pagination:{page:number;limit:number;total:number;pages:number};metadata:{dateFrom:string|null;dateTo:string|null;generatedAt:string;currency:string;isEstimated:boolean;warnings:string[]}}export const reportApi={get:(endpoint:string,query:string)=>apiClient<ReportResponse>(`/reports/${endpoint}${query?`?${query}`:""}`)};
