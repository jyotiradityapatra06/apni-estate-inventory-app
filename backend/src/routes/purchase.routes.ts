import{Router}from"express";import{protect,requirePermission}from"../middleware/auth.middleware";import * as c from"../controllers/purchase.controller";const r=Router();r.get("/",protect,requirePermission("purchases:view"),c.list);r.post("/",protect,requirePermission("purchases:manage"),c.create);r.get("/:id",protect,requirePermission("purchases:view"),c.get);r.post("/:id/send",protect,requirePermission("purchases:manage"),c.send);r.post("/:id/receive",protect,requirePermission("purchases:manage"),c.receive);r.post("/:id/cancel",protect,requirePermission("purchases:manage"),c.cancel);r.post("/:id/payments",protect,requirePermission("financials:manage"),c.pay);r.post("/:id/payments/:paymentId/reverse",protect,requirePermission("financials:manage"),c.reversePayment);export default r;
